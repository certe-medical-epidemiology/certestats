% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detect_farrington.R
\name{detect_farrington}
\alias{detect_farrington}
\alias{n_farrington_clusters}
\alias{has_farrington_clusters}
\alias{has_ongoing_farrington_cluster}
\alias{has_farrington_cluster_before}
\alias{has_farrington_cluster_after}
\alias{autoplot.farrington_clusters}
\title{Detect Aberrations Using the Farrington Algorithm}
\usage{
detect_farrington(
  df,
  column_date = NULL,
  column_patientid = NULL,
  method = "farrington",
  frequency = 52,
  years_back = 5,
  window_width = 7,
  reweight = TRUE,
  alpha = NULL,
  trend = TRUE,
  population_offset = FALSE,
  n_periods = 1,
  past_periods_ignored = NULL,
  threshold_method = "delta",
  case_free_days = 14,
  minimum_cases = 1,
  minimum_duration = 1,
  range = NULL,
  ...
)

n_farrington_clusters(x)

has_farrington_clusters(x, n = 1)

has_ongoing_farrington_cluster(x, dates = Sys.Date() - 1)

has_farrington_cluster_before(x, date)

has_farrington_cluster_after(x, date)

\method{autoplot}{farrington_clusters}(object, ...)
}
\arguments{
\item{df}{Data set. This must consist of \strong{only positive results}. The
minimal data set should include a date column and a patient column. Do not
summarise on patient IDs; deduplication to unique patient-dates is handled
automatically.}

\item{column_date}{Name of the column to use for dates. If left blank, the
first date column will be used.}

\item{column_patientid}{Name of the column to use for patient IDs. If left
blank, the first column resembling \code{"patient|patid"} will be used.}

\item{method}{Detection method to use. One of:
\itemize{
\item \code{"farrington"} (default): the improved Farrington Flexible method
(Noufaily et al., 2012), suitable when >= 3 years of historic data are
available.
\item \code{"ears_c1"}, \code{"ears_c2"}, \code{"ears_c3"}: the CDC EARS methods, suitable
as a fallback for short time series without years of baseline data.
}}

\item{frequency}{Number of observations per year. Use \code{52} for weekly data
(the default) or \code{12} for monthly data.}

\item{years_back}{Number of years back in time to include for the baseline
(Farrington only). Defaults to \code{5} for the flexible method.}

\item{window_width}{Total width of the reference window around the current
period in each reference year (Farrington only). The default of \code{7} means
3 periods before, the current period, and 3 periods after. Internally
converted to the half-size \code{w = round((window_width - 1) / 2)} before
passing to \code{\link[surveillance:farringtonFlexible]{surveillance::farringtonFlexible()}}.}

\item{reweight}{A \link{logical} indicating whether to perform the reweighting
step to down-weight past outbreaks (Farrington only). Defaults to \code{TRUE},
which is the Noufaily et al. (2012) recommendation.}

\item{alpha}{Significance level for the one-sided prediction interval.
Defaults to \code{0.05} for Farrington and \code{0.001} for EARS C1/C2, \code{0.025}
for EARS C3.}

\item{trend}{A \link{logical} indicating whether to include a time trend in the
GLM (Farrington only). Defaults to \code{TRUE}.}

\item{population_offset}{A \link{logical} indicating whether to include a
population offset in the GLM (Farrington only). Defaults to \code{FALSE}. If
\code{TRUE}, a \code{column_population} must be provided or the \code{sts} object must
have a populated \code{populationFrac} slot.}

\item{n_periods}{Number of reference periods in the factor variable for the
baseline (Farrington Flexible only). Defaults to \code{1}, which corresponds to
the original Farrington et al. (1996) definition. Setting this to e.g. \code{10}
expands the reference window, which can be useful for large regions with
more data.}

\item{past_periods_ignored}{Number of recent periods to exclude from the
baseline to avoid influence of ongoing outbreaks (Farrington only).
Defaults to \code{NULL}, which uses the value of \code{w}. Noufaily et al. (2012)
advise \code{26} for weekly data.}

\item{threshold_method}{Method to derive the upper bound. One of \code{"delta"}
(Farrington et al., 1996, the default), \code{"nbPlugin"} (Noufaily et al.,
2012), or \code{"muan"} (extended from Noufaily et al., 2012).}

\item{case_free_days}{Number of case-free days to separate distinct
aberration episodes. Passed to \code{\link[AMR:get_episode]{AMR::get_episode()}}. Defaults to \code{14}.}

\item{minimum_cases}{Minimum number of cases for an aberration episode to be
retained. Defaults to \code{1}.}

\item{minimum_duration}{Minimum number of days (inclusive) for an aberration
episode to be retained. Defaults to \code{1}.}

\item{range}{Index of timepoints to monitor. If \code{NULL} (the default), the
last \code{frequency} timepoints (i.e. the most recent year) are evaluated.}

\item{...}{Additional arguments passed to \code{\link[surveillance:farringtonFlexible]{surveillance::farringtonFlexible()}}
or \code{\link[surveillance:earsC]{surveillance::earsC()}}.}

\item{x}{output of \code{\link[=detect_farrington]{detect_farrington()}}}

\item{n}{minimum number of clusters, defaults to 1}

\item{dates}{date(s) to test whether any cluster currently spans this date.
Defaults to yesterday. Returns a \link{logical} vector with the same length as
\code{dates}.}

\item{date}{a single date to test whether there are any clusters before or
after this date.}

\item{object}{output of \code{\link[=detect_farrington]{detect_farrington()}}}
}
\description{
Detect aberrations (unexpected increases) in surveillance count data using
the Farrington algorithm (Farrington et al., 1996) or its improved flexible
variant (Noufaily et al., 2012). For time series
without sufficient historic baselines, the EARS methods (C1, C2, C3) are
available as a fallback via the \code{method} argument.
}
\details{
The data are internally converted from a line list (one row per isolate or
case) to an aggregated \code{sts} object from the \pkg{surveillance} package,
which is then passed to \code{\link[surveillance:farringtonFlexible]{surveillance::farringtonFlexible()}} or
\code{\link[surveillance:earsC]{surveillance::earsC()}}.

Use \code{\link[=has_farrington_clusters]{has_farrington_clusters()}} to return \code{TRUE} or \code{FALSE} based on the
output, or employ \code{\link[=format]{format()}} to format the result into a summary data frame.
Use \link[=autoplot.farrington_clusters]{autoplot()} for a \code{ggplot2} visualisation,
or \code{\link[=plot]{plot()}} for the base graphics version.
\subsection{Farrington (recommended)}{

The Farrington algorithm is the standard method for automated aberration
detection in European infectious disease surveillance, used by ECDC, Public
Health England, and the Robert Koch Institute (RKI), among others. For each
evaluated time point, a quasi-Poisson GLM is fitted to reference counts from
the same calendar period in previous years. The predicted count and its
overdispersion are used to derive an upper threshold via a
variance-stabilising transformation. An aberration is flagged when the
observed count exceeds this threshold.
\subsection{Farrington Flexible}{

The improved "Flexible" variant (Noufaily et al., 2012) adds reweighting of
past outbreaks so that a historic epidemic does not inflate the baseline and
mask future events.
}

}

\subsection{EARS (fallback for short series)}{

The Early Aberration Reporting System (EARS) methods from the CDC are
Shewhart-type control charts that only require counts from the recent past
(default: 7 time points). They are useful when insufficient historic data
are available for the Farrington approach.
}

\subsection{Aberration clusters}{

After the surveillance algorithm flags individual time points as aberrations,
consecutive (or near-consecutive) aberrations are grouped into clusters using
\code{\link[AMR:get_episode]{AMR::get_episode()}} with the \code{case_free_days} parameter. This produces
operationally useful clusters with start dates, end dates, case counts, and
durations, analogous to \code{\link[=detect_disease_clusters]{detect_disease_clusters()}}.
}
}
\examples{
# generate example line list data spanning several years
set.seed(123)
cases <- data.frame(
  date = sample(seq(as.Date("2018-01-01"),
                    as.Date("2024-12-31"),
                    "1 day"),
                size = 500,
                replace = TRUE),
  patient = sample(LETTERS, size = 500, replace = TRUE)
)

# --- Farrington Flexible (default) ---
result <- detect_farrington(cases)
result
has_farrington_clusters(result)
n_farrington_clusters(result)
format(result)

# check for ongoing cluster
has_ongoing_farrington_cluster(result, Sys.Date() - 1)

# plot the results
plot(result)
if (require("ggplot2")) autoplot(result)

# --- EARS C2 (short baseline) ---
recent <- cases[cases$date >= as.Date("2024-06-01"), ]
result_ears <- detect_farrington(recent, method = "ears_c2")
result_ears
if (require("ggplot2")) autoplot(result_ears)

# --- Farrington with expanded reference window for large regions ---
result_large <- detect_farrington(cases, n_periods = 10,
                                  past_periods_ignored = 26,
                                  threshold_method = "nbPlugin")
result_large
if (require("ggplot2")) autoplot(result_large)
}
\references{
\itemize{
\item Farrington CP, Andrews NJ, Beale AD, Catchpole MA (1996). A statistical
algorithm for the early detection of outbreaks of infectious disease.
\emph{J. R. Statist. Soc. A}, \strong{159}, 547-563.
\item Noufaily A, Enki DG, Farrington CP, Garthwaite PH, Andrews NJ, Charlett A
(2012). An improved algorithm for outbreak detection in multiple
surveillance systems. \emph{Statistics in Medicine}, \strong{32}(7), 1206-1222.
\item Salmon M, Schumacher D, Hohle M (2016). Monitoring count time series in R:
Aberration detection in public health surveillance. \emph{Journal of Statistical
Software}, \strong{70}(10), 1-35.
}
}
\seealso{
\code{\link[=detect_disease_clusters]{detect_disease_clusters()}}, \code{\link[surveillance:farringtonFlexible]{surveillance::farringtonFlexible()}},
\code{\link[surveillance:earsC]{surveillance::earsC()}}
}
